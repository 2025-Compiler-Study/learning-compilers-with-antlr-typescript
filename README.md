# ANTLR MiniC 구현 프로젝트

ANTLR4를 사용하여 C 언어의 부분집합인 MiniC를 구현하며 컴파일러의 동작 원리를 학습하는 프로젝트입니다.

## 학습 목표

MiniC 컴파일러를 직접 구현하면서 컴파일러의 전체 과정을 이해합니다.

## 기술 스택

- **ANTLR4**: Parser Generator
- **TypeScript**: 구현 언어
- **Vitest**: 테스트 프레임워크
- **antlr4ng**: ANTLR TypeScript Runtime

## 설치

```bash
npm install
```

## 구현 로드맵

> 각 단계 진행 시 업데이트 예정입니다.

### Phase 1: ANTLR 기초

- [x] ANTLR 표현 문법 학습
- [x] 도구를 사용해서 TypeScript 코드 생성하기

### Phase 2: Lexer 구현

- [x] ANTLR로 Lexer를 위한 토큰 정의
- [x] Lexer를 사용해서 토큰을 분석해서 반환하는 코드 작성
- [x] Longest Matching 원칙 검증
- [x] 토큰화와 파싱의 차이 이해

📄 [Lexer 상세 명세 보기](./mini-c/Lexer.md)

### Phase 3: Parser 구현

- [ ] ANTLR로 Parser를 위한 문법 정의
- [ ] Listener, Visitor pattern 이해
- [ ] Listener / Visitor 패턴을 활용해서 순회하는 코드 작성
- [ ] Parse Tree -> AST 추출
- [ ] AST -> Semantic 분석

### Phase 4: 실행

- [ ] Semantic 분석 결과를 행동으로 옮기기 (Interpreter)
- [ ] Semantic 분석 결과를 기계어로 생성하기 (Compiler)

## MiniC 언어 명세

### 지원하는 기능

- **타입**: `int`, `char`, `void`
- **제어문**: `if-else`, `while`, `for`, `return`, `break`, `continue`
- **연산자**:
  - 산술: `+`, `-`, `*`, `/`, `%`
  - 관계: `==`, `!=`, `<`, `>`, `<=`, `>=`
  - 논리: `&&`, `||`, `!`
  - 대입: `=`
  - 증감: `++`, `--`
- **배열**: 1차원 배열 선언 및 접근
- **함수**: 함수 정의 및 호출
- **리터럴**: 정수, 문자, 문자열
- **주석**: 한 줄 (`//`), 여러 줄 (`/* */`)

## 프로젝트 구조

```
antlr/
├── mini-c/
├── package.json
└── README.md
```

## 명령어

### Lexer 생성

```bash
npm run antlr -- -o mini-c mini-c/MiniCLexer.g4
```

### 테스트 실행

```bash
npm test              # Watch 모드
npm run test:run      # 한 번만 실행
npm run test:watch    # 명시적 watch 모드
```

## 참고 자료

- [ANTLR 공식 문서](https://www.antlr.org/)
- [antlr4ng GitHub](https://github.com/mike-lischke/antlr4ng)
